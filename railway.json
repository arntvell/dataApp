{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python scripts/init_db.py 2>&1 && python -c 'import main; print(\"Import OK\")' 2>&1 && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} 2>&1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
